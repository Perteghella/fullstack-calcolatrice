README per templates

Metti qui il tuo file `debian-rootfs.tar` (es. ottenuto con `wsl --export Debian C:\wsl\exports\debian-rootfs.tar`).

Non includere il tar nel repository: è solitamente molto grande. Usa il path assoluto nel comando `import_distro.ps1`.
